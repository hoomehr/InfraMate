{
  "name": "json-api-app",
  "version": "1.0.0",
  "description": "Simple JSON API application",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "jest",
    "build": "mkdir -p dist && cp -r src package.json dist/ && cd dist && npm install --production"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.0",
    "body-parser": "^1.20.2",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "jest": "^29.4.3"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 